import React from 'react'
import Header from 'elements/Header/Header'

export const LandingPage = (props) => {
    return (
        <div>
            <Header {...props}></Header>
        </div>
    )
}
